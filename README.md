# cp4d-installation-scripts

> Store your `cpd_vars.sh` or similar `.sh` file with your installation configuration into the folder `./cp4d_config` where it will be read from for the environment variables.
